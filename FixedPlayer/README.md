This directory contains code where identifiability is ensured by fixing one player's skill parameter to 0. AR_MLE.py contains code for 1 experiment, and sweep.py runs a sweep over the weight parameter.

tennis.py performs the method on real-world data.
